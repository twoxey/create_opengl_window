#include "quad_shader_def.h"

out vec2 frag_uv;

void main() {
    vec2 uv = vec2(gl_VertexID & 1, (gl_VertexID >> 1) & 1);
    vec2 pos = uv;
    pos *= size;
    pos += offset;
    pos *= vec2(2) / screen_size;
    gl_Position = vec4(pos, 0, 1);
    frag_uv = uv;
}
